// Server: A menu with every user and a box that allows setting valid verification (can come back to it from chat and vice-versa)
// Client: 1 input box in the middle which allows typing and sending a verification PIN once [if accepted, fo to chat. Otherwise, back to onboarding]

#[macro_export]
macro_rules! initServer {
    ($terminal:ident, $vim_mode: ident, $input:ident, $cursor_pos:ident, $persis_y: ident, $curr_screen: ident, $config: ident) => {
        //TODO
    };
}

#[macro_export]
macro_rules! initClient {
    ($terminal:ident, $vim_mode: ident, $input:ident, $cursor_pos:ident, $persis_y: ident, $curr_screen: ident, $config: ident) => {
        //TODO
    };
}

